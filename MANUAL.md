# 操作マニュアル（先生向け・v0.10）

個別の教育支援計画・個別の指導計画を、AIの力を借りながら作るためのアプリです。
このマニュアルの通りに進めれば、初めての方でも計画書を完成させられます。

---

## 1. はじめに

### 1.1 このアプリでできること
- 個別の教育支援計画・個別の指導計画の下書き作成
- AIによる文案づくり、表現チェック、引継ぎ要約（※AIの文章は必ず先生が確認・修正します）
- 自治体の様式（Excel/Word）への自動差し込みと印刷・PDF出力
- 提出→承認の流れの管理、支援会議メモの共有、引継ぎデータの出力

### 1.2 大切なお約束
- **実名・住所・電話番号は「個人情報」欄にだけ書きます。** AIには送られません（印刷のときだけ使います）
- **AIの文章をそのまま使いません。** 必ず読んで、直して、保存します
- **保護者の確認なしに完成にしません。** 了承日・確認フラグを必ず入力します

### 1.3 画面の構成
| 画面 | できること |
|---|---|
| トップ | 進捗ダッシュボード、年間の目安 |
| 計画 | 計画の一覧・新規作成・複製（年度更新） |
| 計画の詳細 | 入力・AI支援・検証・提出・承認・印刷 |
| 様式・差し込み | 自治体様式の取込・プレビュー・Excel/PDF出力 |
| 監査 | 操作の記録の確認（管理職のみ） |
| ログイン | ログイン・パスワード変更・2段階認証の設定 |

---

## 2. 準備（初回のみ・管理者向け）

### 2.1 必要なもの
- Windowsパソコン（Python 3.12以上、Node.js 22以上）
- プログラム一式（このリポジトリ）

### 2.2 セットアップ手順
1. フォルダを開きます（例：`C:\Users\masan\開発中のアプリ\個別支援＿個別指導計画作成`）
2. バックエンドの準備をします（PowerShellで実行）：
```powershell
pip install -r backend\requirements.txt
python backend\seed_niigata.py
```
3. バックエンドを起動します（このウィンドウは開いたままにします）：
```powershell
python -m uvicorn app.main:app --app-dir backend --reload
```
4. 別のPowerShellを開き、フロントエンドを起動します（初回のみ `npm install`）：
```powershell
cd frontend
npm install
npm run dev
```
5. ブラウザで `http://localhost:3000` を開きます

### 2.3 初期ユーザと初期設定
- 初期ユーザ：`admin` / `manager` / `teacher` / `viewer`（パスワードは `○○○123`、例：`teacher123`）
- 自分で始める場合はログイン画面の「新規登録」から担任アカウントを作成できます（管理職の関与なし）
- 委員会許可などで簡易運用する場合は `backend/.env` に `SIMPLE_MODE=1`（初期PW警告なし・セッション30日）
- 使用者本人のAPIキーで使う場合は、「設定」画面で発行し、ログイン画面の「APIキーでログイン」から利用
- 最終段階の起案（承認）のみ管理職が行います
- 初回ログイン時に「初期パスワードのままです」と警告が出ます。**必ず全員パスワードを変更**してください（ログイン画面の下部）
- 作業終了時は右上の「ログアウト」を押してください（セッションも無効化されます）
- 管理職は2段階認証（確認コード発行→認証アプリに登録）を有効にしてください
- 終了時は2つのPowerShellを `Ctrl+C` で止めます

---

## 3. 計画を作る（基本の流れ）

### 3.1 新規作成
1. ログインします（例：`teacher` / `teacher123`）
2. 「計画」を開き、管理番号・学年・在籍形態・学校名を入力して「新規作成」を押します
   - 管理番号は児童を特定しない番号です（例：`S-2026-001`）。**実名はここに書かず**、計画内の氏名欄に書きます

### 3.2 内容を入力する
1. 一覧から計画の「開く」を押します
2. 年度当初は管理職が「名簿一括取込」で下書きをまとめて作れます（CSV：管理番号,学年,在籍,学校）
2. 各欄を入力します。欄は3つのグループに分かれています
   - **基本・文科省項目**：好き・得意、苦手・配慮点、願い、長期目標、短期目標、手立て、評価など
   - **新潟様式項目**：生育歴、生活の様子、学校の支援、関係者（家庭・地域・福祉・医療）の支援など
   - **指導表**：実態・目標・方法・評価を9行まで（使う行だけでOKです）
   - **引継ぎ・同意**：引継ぎ先、情報提供の同意、糊しろ（4月のおさらい課題）など
3. ［個人情報・AI送信外］と書かれた欄（氏名・住所・電話など）は印刷専用で、AIには送られません
4. 文章のコツ（画面のチェックも同じ観点です）
   - 短い文・ですます調で書く
   - 専門用語（アセスメント、手立て、所見など）は具体的な言い方に直す
   - 目標には基準を入れる（例：「予定表を見て5分で支度する」）
   - できないことより、目指す姿を肯定的に書く

### 3.3 保存と検証
1. 「保存・検証」を押すと、不足・表現・整合性が自動チェックされます
2. 保存のたびに変更履歴が残り、「変更履歴」から過去の版に戻せます（復元も履歴に記録）
2. 指摘が出たら内容を直して再保存します（`blocked=true` のままだと提出できません）
3. 指摘の見方：`error`（必須・要修正）、`warn`（直すと良くなる）、`info`（ヒント）
4. 「次回見直し日」を入れると、トップ画面で期限切れ・30日以内のアラートが出ます

---

## 4. AI支援を使う（必ず確認・修正する）

### 4.1 下書きを作る
1. 計画詳細の「AI支援」カードで、作りたい種類（長期目標・短期目標・引継ぎ要約）を選びます
2. 反映先の欄を選び「下書き生成」を押します
3. 出てきた文案を読んで「文案を反映」を押すと、選んだ欄に入ります
4. **そのままにせず、自分の言葉に直してから保存**してください

### 4.2 文章を磨く（AI推敲）
1. 各入力欄の「AI推敲」を押すと、修正案（専門用語の言い換えなど）が表示されます
2. よければ「修正案を反映」を押します。意味が変わっていないか必ず確認してください

### 4.3 全体チェックと糊しろ提案
- 「全体チェック」：表現・やさしい日本語・目標の具体性をまとめて確認します
- 「糊しろ提案」：4月最初の課題案（得意なことから始める等）を3件まで作ります。「糊しろ欄に反映」で取り込めます
- AIには匿名化した内容だけが送られます。送信記録は監査ログに残ります

---

## 5. 提出・承認する

1. 内容がそろったら「提出(review)」を押します（検証エラーがあると提出できません）
2. 管理職は内容を確認し「承認(approved)」を押します
3. 保護者の合意は「合意の記録」で残します（対面・共有リンク・書面＋内容ハッシュ、保護者確認も自動付与）
3. 承認後に直す必要が出たら、管理職のみ修正できます（修正すると自動で review に戻ります）
4. 年度更新は一覧の「複製（年度更新）」を使います（確認・同意はリセット、会議メモは引継ぎ）

---

## 6. 様式に印刷する

### 6.1 新潟県様式を使う
1. 「様式・差し込み」を開きます（新潟様式 `niigata01` は登録済みです）
2. 様式を選び、差し込み元の `plan_id` を入力します（一覧のURL `plans/○○` の○○部分）
3. 「プレビュー」で空欄・未割当を確認します
4. 「Excel出力」または「PDF出力（新潟様式）」を押してファイルを取得します
5. Excelで開いて体裁を整え、印刷→保護者に確認してもらいます（印欄は手押印です）

### 6.2 他の自治体様式を使う
1. 「自治体様式の取込」でExcel/Wordファイル（10MB以内）をアップロードします
2. 「候補を自動割当」を押して内容を確認し、必要なら手で直して「保存」します
3. 以降は6.1の手順3〜5と同じです

---

## 7. 引き継ぐ・共有する

- **支援会議メモ**：計画詳細の下部から申送り・相談を投稿できます（関係者共有用、個人情報の書込み注意）
- **日々の指導記録**：目標と紐付けて記録を積み重ね、評価の材料にします
- **引継ぎCSV/JSON**：計画詳細のボタンで出力します。**保護者の同意フラグがないと出力できません**
- **保護者共有リンク**：提出以降の計画に期限付きリンクを発行し、保護者に説明文を見てもらえます。閲覧は記録され、取消も可能です
- 進学・転出先へ渡すときは、同意の範囲・方法を必ず確認してください

---

## 8. 管理者向け

- **監査ログ**：`/audit` で操作・AI利用・出力の記録を確認、CSV出力できます（管理職のみ）
- **バックアップ**：`python backend/backup.py backup`（週1推奨、外部媒体へ複写）。復元は `restore <zip> --force`
- **健康状態**：`http://localhost:8000/api/health` でDB・フォント・空き容量を確認できます
- **パスワード**：初期PWの変更徹底、年1回の変更推奨。2段階認証は管理職必須にしてください
- 詳細は `docs/operations.md` を参照してください

---

## 9. 困ったときは

| 症状 | 対処 |
|---|---|
| ログインできない・試行超過 | 5分待って再試行。管理者に確認 |
| 提出できない | 保存時の指摘（error）を解消する |
| 差し込みで空欄が多い | プレビューで空欄セルを確認し、計画の対応欄を入力する |
| PDFの日本語がおかしい | `/api/health` の `pdf_font` を確認。`pip install japanize-matplotlib` |
| 画面が真っ白 | バックエンド（8000）とフロント（3000）の両方が起動しているか確認 |
| AIが応答しない | 既定は外部送信なしの試作応答です。外部LLMは `backend/.env` の設定が必要（`docs/llm-selection.md`） |

---

## 付録：年間の目安（新潟ハンドブックより）
- 3〜4月：相談・引継ぎ → 4月：実態把握・合意形成 → 4〜5月末：作成・校内委員会
- 5月末まで：保護者へ説明・合意 → 随時：実施・修正 → 年度末・学期末：評価・次年度へ引継ぎ
